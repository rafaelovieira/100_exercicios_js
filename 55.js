/* 55 - Crie um script que declare uma função que receba um array de números como parâmetro 
e retorne um novo array onde cada número seja dividido por 2. */

const numeros = [2, 4, 6, 8, 10]

const dividindoPor2 = numeros.map(numero => numero / 2)

console.log(dividindoPor2)