/* 24 - Crie um script que declare um array de strings e utilize a função join() para concatenar todas as strings em uma única string, 
separadas por vírgulas. Imprima o resultado. */

const palavras = ["Eu", "amo", "programar"]

const concatenarStringsdeUmArray = palavras.join(", ")

console.log(concatenarStringsdeUmArray)